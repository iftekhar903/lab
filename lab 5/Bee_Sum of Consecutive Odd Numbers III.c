#include<stdio.h>
int main()
{
    int n, X, Y;
    scanf("%d", &n);
    for(int i=0; i<n; i++)
    {
        scanf("%d %d", &X, &Y);
        for(int j=0; j<=(Y+1); j++)
        {
            if()
        }
    }


    return 0;
}
